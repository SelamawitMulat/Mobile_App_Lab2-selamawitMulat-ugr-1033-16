I have added a folder named go_router for Assignment #2, and it contains all three projects.

The main folder includes:

The first four projects
The go_router folder (which contains all three projects from Lab 2)

NB: Each project has its own screenshots stored in a folder named screenshots.